export const nativeFeedback = {
  impact: async (style: string = 'LIGHT') => {
    // No-op (Mobile Bridge Removed)
  },
  notification: async (type: 'SUCCESS' | 'WARNING' | 'ERROR') => {
    // No-op (Mobile Bridge Removed)
  },
  selection: async () => {
    // No-op (Mobile Bridge Removed)
  }
};

export const setupNativeUI = async () => {
  // No-op (Mobile Bridge Removed)
};
